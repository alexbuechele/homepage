//this file is broken, just notes

/*

MessageLog({
    name: req.body.name,
    email: req.body.email,
    message: req.body.email,
    ipaddress: req.headers["x-real-ip"]
});

MessageLog.save(){
    db = require('../db');
}

*/